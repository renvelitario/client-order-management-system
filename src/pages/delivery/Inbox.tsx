const Inbox = () => {
  return (
    <section className="container" aria-labelledby="delivery-inbox-title">
      <h1 id="delivery-inbox-title">Inbox</h1>
      <p>You are all caught up. New delivery updates will appear here.</p>
    </section>
  );
};

export default Inbox;
